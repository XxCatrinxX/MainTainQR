[phases.setup]
nixPkgs = ["php", "nodejs_20", "npm"]

[phases.build]
cmds = [
    "composer install --no-dev --optimize-autoloader",
    "php artisan adminlte:install --only=main --force",
    "npm install",
    "npm run build"
]

[start]
cmd = "php artisan serve --host 0.0.0.0 --port $PORT"